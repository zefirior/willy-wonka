[tool.poetry]
name = "willy-wonka"
version = "0.1.0"
description = ""
authors = ["Daniil Galiev <liinda@yandex-team.ru>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^3.0"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
